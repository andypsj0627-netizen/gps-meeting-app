# GPS Meeting App — 개발 룰

프로젝트 목적, 컨셉, 로드맵은 [`README.md`](./README.md) 참고.

## 세션 시작 시 체크리스트
1. `README.md`를 읽고 프로젝트 목적과 현재 Phase 파악
2. `PROJECT_LOG.md`에서 최근 작업 내용 확인
3. 이어서 작업할 항목 사용자에게 제안

---

## 피처 개발 워크플로우

새 기능을 추가할 때 아래 순서를 따른다. 각 단계에서 명시된 에이전트를 호출할 것.

### 1단계 — 탐색 (필요 시)
기존 코드와 연관된 작업이라면 먼저 맥락을 파악한다.
- 에이전트: `feature-dev:code-explorer`
- 목적: 관련 파일, 패턴, 의존성 파악

### 2단계 — 설계
구현 전 청사진을 먼저 만든다. 파일 목록, 데이터 흐름, 컴포넌트 구조 포함.
- 에이전트: `feature-dev:code-architect`
- 산출물: 구현할 파일 목록 + 각 역할 + 빌드 순서
- **사용자 승인 후 3단계로 진행**

### 3단계 — 구현
설계 청사진을 바탕으로 Claude가 직접 코드를 작성한다.
- dart analyze 훅이 자동으로 각 파일 저장 시 실행됨
- 에러 발생 시 즉시 수정 후 진행

### 4단계 — 테스트 작성
구현한 로직에 대한 단위/위젯 테스트를 반드시 작성한다.
- 위치: `test/` 디렉토리, 파일명은 `*_test.dart`
- 테스트 없이 다음 단계로 넘어가지 않는다

### 5단계 — flutter test (하드 게이트)
테스트를 실제로 실행해서 pass 여부를 기계가 판단한다.
- 커맨드: `flutter test`
- **실패 시 커밋 불가** — 세션 종료 시 Stop 훅이 자동으로 강제함
- AI 리뷰보다 이 단계가 더 신뢰도 높음

### 6단계 — 코드 리뷰 (보조)
AI가 놓칠 수 있는 설계 관점의 문제를 점검한다.
- 커맨드: `/code-review`
- 체크 항목: 아키텍처 일관성, 보안, 가독성
- flutter test를 통과했어도 이 단계는 생략하지 않는다

### 7단계 — 커밋
모든 게이트 통과 후 커밋한다.
- 커맨드: `/commit`
- 커밋 메시지는 `feat:` / `fix:` / `refactor:` 접두어 사용

---

### 검증 계층 (신뢰도 순)
| 단계 | 도구 | 판단자 | 거짓말 가능성 |
|------|------|--------|--------------|
| 정적 분석 | dart analyze | 기계 | 없음 |
| 테스트 실행 | flutter test | 기계 | 없음 |
| 코드 리뷰 | /code-review | AI | 있음 (보조로만 사용) |

### 워크플로우 요약
```
탐색(선택) → 설계 → [승인] → 구현 → 테스트 작성 → flutter test → 코드 리뷰 → 커밋
```
